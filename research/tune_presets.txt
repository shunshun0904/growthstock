all
all_no_cap
# 探索するプリセット。1行1つ。
#
# 公平な比較のため、比べる2本は必ず同じ条件で探索し直す。
# 片方だけ探索済みのまま比べると、特徴量の差ではなく
# パラメータの差を見ることになる（実際それで誤った結論を出しかけた）。
#
#   all         全部入り（136列）
#   all_no_cap  all から log_market_cap だけ抜いたもの（135列）
