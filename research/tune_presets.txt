all
all_no_sector_index
all_no_market
# 探索するプリセット。1行1つ。
#
# 3セットとも同じ条件で探索する。探索していないセットを混ぜると、
# 特徴量の差ではなくパラメータの差を見ることになる。
#
#   all                  全部入り
#   all_no_sector_index  業種指数4列を抜いた版
#   all_no_market        市場環境11列を抜いた版
#
# 業種指数について:
#   rel_sector_20 は日付内で有意（AUC 0.5686 / CI[0.5471,0.5895]、全体4位）
#   だが、ret_20d 単独（0.5967）に負けている。しかも ret_20d と r=0.9632。
#   業種指数の20日リターンは p5 -4.5% / 中央 +3.1% / p95 +12.1% と幅が狭く、
#   高値更新日の銘柄自身のリターンに対して小さいので、引いても元が残る。
#   単独で効かなくても組み合わせで効く可能性は残るので、差で測る。
