all
all_no_market
# 探索するプリセット。1行1つ。
#
# 母集団を 12,484 -> 22,909 に広げたので、以前の探索結果は使えない
# （行数が倍近く違うと最適な葉の数・min_child_samples が変わる）。
#
# all_no_market を並べる理由:
#   市場環境の特徴量を2列から11列に増やした（日経225・金・グロース250・
#   TOPIX の局面）。これらは「その日は全銘柄同じ値」なので、
#   日付ごとの正例率の水準しか動かせない。しかも日付は1,961しかなく、
#   ラベルが60日先を見るので隣り合う日は強く相関する。
#   効いているのか過学習なのかは、market を抜いた版との差でしか分からない。
#   同じ条件で探索していないと「特徴量の差」ではなく
#   「パラメータの差」を見ることになるので、両方とも探索する。
