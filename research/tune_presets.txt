all_plus
all_no_sector_index
all_no_market
# 探索するプリセット。1行1つ。
#
# **先頭は本番の列（features.DEFAULT_PRESET）。** 本番モデルは学習する列と
# 同じ列で探索したパラメータしか使わない（train_production.py が確かめて止める）。
# 運用者の指示（2026-09-23）「パラメータチューニングする際も205 全特徴量を
# 使ったモデルでお願いします。でないとチューニングするいみがないので。
# oofで最良にすることがチューニングの目的ではないです」。
# 153列 -> 205列 に切り替えたとき、ここだけ all（153列）のまま残っていた。
#
#   all_plus             本番（205列）
#   all_no_sector_index  研究用の比較（153列から業種指数4列を抜いた版）
#   all_no_market        研究用の比較（153列から市場環境11列を抜いた版）
#
# 以下は 153列（all）時代の説明。
# ラベルをボラ正規化（VOL_NORM_K=1.2）に変えたので、旧ラベルで探索した
# パラメータは使えない。3セットとも探索し直す。
# 旧ラベルの探索結果は research/lgbm_params_fixed20.json に退避してある。
#
#   all                  全部入り
#   all_no_sector_index  業種指数4列を抜いた版
#   all_no_market        市場環境11列を抜いた版
#
# 地合い11列・業種指数4列は旧ラベルでは効果を検出できなかったが、
# ラベルが変わればモデルが使う情報も変わりうるので測り直す。
