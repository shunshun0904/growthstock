no
# 1行目が "yes" のときだけ学習・評価（train_model / walkforward /
# stratified_eval / within_date_signal）を実行する。
#
# 既定は no。EDA で特徴量の欠損・異常値を確認し、
# 利用者が明示的に許可してから yes にする。
# 特徴量がおかしいまま学習しても結果が無駄になるため、
# 「EDA を見る前に学習が走る」ことがないよう既定を no に固定している。
#
# EDA だけ回したいときは no のまま実行する（データセット構築 →
# カバレッジ → EDA → 指標検証 → ラベル検証チャート まで進む）。
