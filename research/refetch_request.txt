fins
# 1行目に --reset に渡す種別を書く（スペース区切り）。空行なら通常の差分取得。
#
# 決算の取得列を全項目に変更したため、決算のみ取り直す。
# 既存 parquet には BPS / ShEq / NC* 系が入っておらず、
# manifest 上は「取得済み」なので incremental では永久に取り直されない。
# 株価は触らない（取り直すと数時間かかる）。
#
# 2026-09-05 決算を全項目取得に変更 (BPS/ShEq/TA/NC* を保持)
